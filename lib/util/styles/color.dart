import 'package:flutter/material.dart';

const LOGIN_backgroundColor = Color.fromRGBO(227, 227, 227, 1);
const LOGIN_submitButtonColor = Color.fromRGBO(2, 75, 128, 1.0);

const LOGIN_imageBackgroundColor = Color.fromRGBO(203, 35, 33, 0.8);

const ROLE_boxBorderColor = Color.fromRGBO(2, 75, 128, 1);
